<%= form_for(@topic_date) do |f| %>
  <% if @topic_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic_date.errors.count, "error") %> prohibited this topic_date from being saved:</h2>

      <ul>
      <% @topic_date.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :topic %><br />
    <%= f.text_field :topic %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
